
#include<stdio.h>
int max(int x, int y)
{
	if (x > y)
		return x;
	else
		return y;
}
int main()
{
	int a = 0; 
	int b = 0;
	scanf_s("%d %d", &a, &b);
	int m =max(a, b);
	printf("%d\n", m);



	return 0;
}



//先声明后调用
int max(int x,int y);//声明
int main()

{
	int a = 0;
	int b = 0;
	scanf_s("%d %d", &a, &b);
	int m = max(a, b);
	printf("%d\n", m);
	return 0;
}
int max(int x,int y)
{
	if (x > y)
	{
		return x;
	}
	return y;
}
